#include <stdio.h>
#include <math.h>
#include <string.h>


int main()


{
      int length=0;
      char seq[10000];
      int i=0;
      int count;

     while ( scanf ("%s", seq) == 1 )
     {
       for (i=0; i<10000; i++);
       count++;
       printf(" The number of sequence(s) is %d\n", count);
     }

}
